import 'package:flutter_riverpod/flutter_riverpod.dart';

final setupCompleteProvider = Provider<bool>((ref) {
  throw UnimplementedError('setupCompleteProvider muss in InitService per override gesetzt werden');
});